---
import { Mail, Phone, MapPin, Facebook, Twitter, Instagram, Youtube } from 'lucide-astro';
import { Image } from 'astro:assets';
import logoImage from '../assets/images/logos/Xuper_play_logo.webp';

const currentYear = new Date().getFullYear();

const footerLinks = {
  producto: [
    { name: 'Características', href: '#caracteristicas' },
    { name: 'Planes y Precios', href: '#planes' },
    { name: 'Descargas', href: '#descargar' },
    { name: 'Actualizaciones', href: '#' },
  ],
  soporte: [
    { name: 'Centro de Ayuda', href: '#faqs' },
    { name: 'Guía de Instalación', href: '#instalacion' },
    { name: 'Contacto', href: '#contacto' },
    { name: 'Estado del Servicio', href: '#' },
  ],
  legal: [
    { name: 'Términos de Servicio', href: '#' },
    { name: 'Política de Privacidad', href: '#' },
    { name: 'Cookies', href: '#' },
    { name: 'Legal', href: '#' },
  ],
};

const socialLinks = [
  { name: 'Facebook', icon: Facebook, href: '#' },
  { name: 'Twitter', icon: Twitter, href: '#' },
  { name: 'Instagram', icon: Instagram, href: '#' },
  { name: 'YouTube', icon: Youtube, href: '#' },
];
---

<footer id="contacto" class="bg-gray-900 text-gray-300">
  <!-- Main Footer Content -->
  <div class="container mx-auto px-4 py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-12">
      <!-- Brand Section -->
      <div class="lg:col-span-2">
        <div class="mb-6">
          <Image src={logoImage} alt="XuperPlay" class="h-12 w-auto mb-4" loading="lazy" />
          <p class="text-gray-400 mt-4 leading-relaxed">
            Tu plataforma de streaming favorita con miles de canales en vivo, películas, series y contenido on-demand. Entretenimiento sin límites, donde quieras, cuando quieras.
          </p>
        </div>

        <!-- Contact Info -->
        <div class="space-y-3">
          <div class="flex items-center gap-3">
            <Mail size={20} class="text-primary-500" />
            <a href="mailto:soporte@xuperplay.com" class="hover:text-white transition-colors">
              soporte@xuperplay.com
            </a>
          </div>
          <div class="flex items-center gap-3">
            <Phone size={20} class="text-primary-500" />
            <a href="tel:+1234567890" class="hover:text-white transition-colors">
              +1 (234) 567-890
            </a>
          </div>
          <div class="flex items-start gap-3">
            <MapPin size={20} class="text-primary-500 mt-1 flex-shrink-0" />
            <span>
              123 Streaming Avenue<br />
              Digital City, DC 12345
            </span>
          </div>
        </div>
      </div>

      <!-- Links Columns -->
      <div>
        <h4 class="text-white font-bold text-lg mb-4">Producto</h4>
        <ul class="space-y-2">
          {footerLinks.producto.map((link) => (
            <li>
              <a
                href={link.href}
                class="hover:text-white hover:translate-x-1 inline-block transition-all duration-300"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="text-white font-bold text-lg mb-4">Soporte</h4>
        <ul class="space-y-2">
          {footerLinks.soporte.map((link) => (
            <li>
              <a
                href={link.href}
                class="hover:text-white hover:translate-x-1 inline-block transition-all duration-300"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="text-white font-bold text-lg mb-4">Legal</h4>
        <ul class="space-y-2">
          {footerLinks.legal.map((link) => (
            <li>
              <a
                href={link.href}
                class="hover:text-white hover:translate-x-1 inline-block transition-all duration-300"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-gray-800">
    <div class="container mx-auto px-4 py-6">
      <div class="flex flex-col md:flex-row items-center justify-between gap-4">
        <!-- Copyright -->
        <p class="text-gray-500 text-sm text-center md:text-left">
          © {currentYear} XuperPlay. Todos los derechos reservados.
        </p>

        <!-- Social Links -->
        <div class="flex items-center gap-4">
          {socialLinks.map((social) => {
            const Icon = social.icon;
            return (
              <a
                href={social.href}
                class="p-2 bg-gray-800 hover:bg-primary-600 rounded-lg transition-all duration-300 hover:scale-110"
                aria-label={social.name}
              >
                <Icon size={20} />
              </a>
            );
          })}
        </div>
      </div>
    </div>
  </div>
</footer>
